/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Netlify handles Next.js automatically with the plugin
  // Don't use standalone output for Netlify
};

module.exports = nextConfig;

